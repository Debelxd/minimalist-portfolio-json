---
import Section from "../Section.astro";
import { competences } from "@cv";
---

<Section title="Competencias">
  <ul>
    {
      competences.map(({ name, area }) => {
        return (
          <li>
            <article>
              <header>
                <div>
                  <h3>
                    <span>•</span>
                    {name}
                  </h3>
                </div>
              </header>

              <footer>
                <p>{area}</p>
              </footer>
            </article>
          </li>
        );
      })
    }
  </ul>
</Section>
<style>
  ul {
    display: inline-flex;
    flex-direction: column;
    gap: 12px;
    flex-wrap: wrap;
  }

  li {
    align-items: center;
    border-radius: 6px;
    display: flex;
    font-size: 0.8rem;
    font-weight: 500;
    gap: 4px;
    /*
    padding: 0.2rem 0.6rem;
    */
  }

  p {
  }
</style>
